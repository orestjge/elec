name: edge_sjis
description: >-
  エッヂ (eddist) / 5ch 互換掲示板のワイヤフォーマットである Windows-31J (CP932) の
  エンコード・デコード。Flutter 非依存の純 Dart。
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  # Windows-31J (CP932) 実装。ブラウザと同じ変換表を持つ。
  # charset パッケージは使わないこと (理由は README.md)。
  jis0208: ^1.0.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
